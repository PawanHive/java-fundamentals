## Time Complexity of BST: 
-  `O(H) = O(n)` (even in worst case)
- here, `O(H)` means Big of Height 

## Strategy

- Most problems will be solved using `recursion` i.e. by dividing into `subproblems` & making recursive calls on subtrees.