
# Stack Overflow

defining **base case** is very **important** otherwise **stack overflow** will happen means there is no space in memory to store functions parameter because it will run for infinite times if we didne define **base case**