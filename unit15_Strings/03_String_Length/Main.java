

public class Main {
  public static void main(String[] args) {
    String fullName = "Tony Stark";
    System.out.println(fullName.length());

    /*
    Note:
    `length()` is a **method**, so we use `()`, but to measure arrays length don't use '()'
    */
  }
}
